stream-workflow:
  type: Workflow
  desc: |
    Stream workflow that run all process groups.
  params:
    stream: str
    run_mode: str
  jobs:
    start-job:
      stages:
        - name: Get stream info
          echo: "Start getting stream info"
#          uses: tasks/get_stream_info@v1
#          with:
#            name: "${{ params.stream }}"
    main-job:
      stages:
        - name: Start stream
          echo: "Start running stream"
    end-job:
      stages:
        - name: Clear log
          echo: "Clear logging"
        - name: Alert
          echo: "Alert with mail or noti-services"
